- investigate the possibility to define the super sort Obj. This would enable defining relations like "void_r". 
  In fact, that should work if we had anonymous variable but we don't. 
- Implement tests
- Use logs instead of homemadae warnings. 
- allow &eq(..., xlim)
- Place actions between states in the python printer
- Change all the K, T to I. 
- enable not only &eq, but an improved version where we can specify the whole object directly. 
  But what would we do with height, ... so the derived quantities? No, this is fine, we simply define left, bottom, height, width
- enable several names for the same rel, like properPart/in
- to which extend isn't it better to organize files by means of relations, like left.lp instead of by sorts? Maybe now that they 
  are supporting overloading, maybe is this useful. 